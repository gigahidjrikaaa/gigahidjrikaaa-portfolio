# Frontend environment variables
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
# Optional: Cloudinary API key is an identifier (NOT the API secret).
# Only needed if you use Cloudinary's Media Library widget in the browser.
NEXT_PUBLIC_CLOUDINARY_API_KEY=
# Unsigned upload preset (safe to expose if restricted in Cloudinary settings)
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=
